export default class Voice {
	private callNativeSync(op: string, ...args: any[]) {
		/**
		 * Deno.openPlugin(...)
		 */
	}

	private async callNative(op: string, ...args: any[]) {
		/**
		 * Deno.openPlugin(...)
		 */
	}
}
